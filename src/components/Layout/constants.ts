export const borderEnum = {
  all: { border: '1px solid var(--gray-300)' },
  top: { borderTop: '1px solid var(--gray-300)' },
  bottom: { borderBottom: '1px solid var(--gray-300)' },
  left: { borderLeft: '1px solid var(--gray-300)' },
  right: { borderRight: '1px solid var(--gray-300)' },
} as const
